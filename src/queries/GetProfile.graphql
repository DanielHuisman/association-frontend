# import '../fragments/Member.graphql'
# import '../fragments/Provider.graphql'

query GetProfile {
    me {
        ...MemberFragment

        providers {
            values {
                ...ProviderFragment
            }
        }
    }
}
