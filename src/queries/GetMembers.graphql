# import '../fragments/Member.graphql'
# import '../fragments/Mandate.graphql'

query GetMembers {
    members {
        values {
            ...MemberFragment

            mandates(
                where: {
                    status: {
                        equals: ACCEPTED
                    }
                }
            ) {
                values {
                    ...MandateFragment
                }
            }
        }
    }
}
