# import '../fragments/Mandate.graphql'
# import '../fragments/Member.graphql'

query GetMandates {
    mandates {
        values {
            ...MandateFragment

            member {
                ...MemberFragment
            }
        }
    }
}
